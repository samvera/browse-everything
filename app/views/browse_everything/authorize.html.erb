<script>
  var authorizePromise = new Promise(function(resolve, reject) {
    window.opener.postMessage(<%= JSON.generate(@auth_token).html_safe %>, '*');
    return resolve();
  });
  authorizePromise.then(function() {
    window.close();
  });
</script>
